import { expect } from 'chai';

describe('My first test', () => {

  describe('Open x', () => {

    describe('When something something', () => {
      beforeAll(async () => {
      });

      it('Then it should show the digiboard tool button', async () => {
          console.log('hello world');

      });
    });

    describe('When the digiboard-tool is opened', () => {
      beforeAll(async () => {

      });

      it('Then it should show the tool', async () => {

      });

    });
  });

});
